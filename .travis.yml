sudo: false
language: node_js
node_js:
  - 8
cache:
   directories:
      - node_modules
      - $HOME/.meteor/

before_install: 
  - if [[ `npm -v` != 5* ]]; then npm i -g npm@5; fi
  - curl https://install.meteor.com | /bin/sh

script:
  - npm run spacejam
